#include "BackgroundView.h"

void BackgroundView::drawGrid(double atX, double atY, SDL_Surface * toSurface) {
	return;
}
void BackgroundView::drawBackground(double atX, double atY, SDL_Surface * toSurface) {
	this->drawGrid(atX,atY,toSurface);
	return;
}